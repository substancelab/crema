- content_for(:page_title) { @customer }

- content_for(:page_header)
  = render( \
    PageHeading::Component.new( \
      :title => "Editing customer", \
      :actions => [ \
        PageHeading::DeleteActionComponent.new(:label => "Delete", :url => customer_path(@customer)),
      ] \
    ))

.mt-6.max-w-8xl.mx-auto.px-4.sm:px-6.lg:px-8
  == render "form", :customer => @customer
